import React, { FC } from "react"
import { Text } from "@chakra-ui/core/dist"
import { RouteComponentProps } from "@reach/router"

export const Complaints: FC<RouteComponentProps> = () => {
  return <Text>Complaints</Text>
}
