import React, { FC } from "react"
import { Box, Text } from "@chakra-ui/core/dist"
import { RouteComponentProps } from "@reach/router"

export const WorldMap: FC<RouteComponentProps> = () => {
  return <Box size="500px">World Map</Box>
}
